import React from 'react'

const TitleChangetheme = (props) => {
    return (
        <div className={props.className} style={{width: '100%', height: '59px', backgroundColor: "#fff", overflow: 'auto'}}>
            <span className='_384dsk' style={{fontSize: 20, fontWeight: 700}}>Color</span>
        </div>
    )
}

export default TitleChangetheme
