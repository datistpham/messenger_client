import React from 'react'


const OptimizeImage = (props) => {
    
    return (
        <div className={props.className}>
            <img className='_39ewifdb _weksdpols' src={props.image} alt="Can't display" style={{maxWidth: '100%', width: 335,minWidth: 200, borderRadius: 10, objectFit: 'cover'}} />
        </div>
    )
}

export default OptimizeImage
